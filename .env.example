# Database
DATABASE_URL="postgresql://user:password@localhost:5432/Immerse"

# Next.js
NEXT_PUBLIC_API_URL="http://localhost:3000/api"

# Authentication (to be configured)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"

# Testing (only enable in development/testing environments)
# When TEST_MODE="true", authentication bypasses Prisma database queries
# and uses TEST_USER credentials directly for faster test execution
TEST_MODE="false"  # Set to "true" to enable test mode (NEVER in production)
TEST_USER_EMAIL="admin@example.com"
TEST_USER_PASSWORD="admin"
TEST_USER_NAME="Test Superuser"

# Optional: External Services
# AWS_ACCESS_KEY_ID=""
# AWS_SECRET_ACCESS_KEY=""
# AWS_REGION=""
